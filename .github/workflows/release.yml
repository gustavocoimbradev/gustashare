name: Build and release GustaShare

# So dispara manualmente (aba Actions -> Run workflow). A versao so muda
# quando esse workflow roda de verdade — "npm run publish" so envia
# codigo, nao mexe em versao nem builda nada.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Bump version
        run: npm run bump-version

      - name: Read version
        id: version
        shell: pwsh
        run: echo "version=$(node -p "require('./package.json').version")" >> $env:GITHUB_OUTPUT

      - name: Setup Rust (módulo nativo de captura)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Build módulo nativo (native/gustashare-capture)
        run: npm run build-native

      - name: Build app (Windows real, sem wine)
        run: npm run build

      - name: Atualizar latest.json
        run: node scripts/sync-update-server.js

      - name: Build web (base absoluta, pra Vercel)
        run: npm run build:web

      - name: Copiar .exe pra pasta publicada
        shell: bash
        run: |
          ls -lh release/GustaShare-Portable.exe
          cp release/GustaShare-Portable.exe dist/GustaShare-Portable.exe

      - name: Deploy na Vercel
        shell: bash
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        # Root Directory do painel tem que estar VAZIO (raiz do repo).
        # Publicamos só o `dist/` (app web + latest.json + .exe) num
        # staging, senão a CLI manda node_modules/target (~750MB).
        run: |
          ls -lh dist/GustaShare-Portable.exe dist/latest.json
          staging="$(mktemp -d)"
          mkdir -p "$staging/.vercel"
          cp update-server/.vercel/project.json "$staging/.vercel/project.json"
          cp -r dist/. "$staging/"
          cd "$staging"
          npx vercel deploy --prod --yes --token="$VERCEL_TOKEN"

      - name: Commitar versão + latest.json
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add package.json update-server/latest.json
          git diff --cached --quiet || git commit -m "chore: release v${{ steps.version.outputs.version }} [skip ci]"
          git push
